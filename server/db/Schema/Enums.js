// Only stored ENUM use across multiple objects here
// ENUM that only use one is defined on the spot in the Schema

const TAX_STATUS = ['NON_RETIREMENT', 'PRE_TAX_RETIREMENT', 'AFTER_TAX_RETIREMENT'];
const FILING_STATUS = ['SINGLE', 'MARRIEDJOINT'];

export { TAX_STATUS, FILING_STATUS };