import React from "react";
import styles from "./Event.module.css";

const Event = () => {
  return (
    <section className={styles.events}>
      <h2>Event Series</h2>
      <p>No events added. Edit the scenario to add events.</p>
    </section>
  );
};


export default Event;